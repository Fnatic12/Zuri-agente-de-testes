# vwait.features.visual_qa.infrastructure

Infrastructure implementations should depend on `vwait.features.visual_qa.application.ports.protocols`.

This package is the place for concrete adapters (embeddings, FAISS, pixel adapter, LLM, storage).
