# visual_qa.infrastructure

Infrastructure implementations should depend on `visual_qa.application.ports.protocols`.

This package is the place for concrete adapters (embeddings, FAISS, pixel adapter, LLM, storage).
