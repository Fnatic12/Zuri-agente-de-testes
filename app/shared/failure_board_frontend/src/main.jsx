import React from "react";
import ReactDOM from "react-dom/client";

import FailureBoard from "./FailureBoard";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FailureBoard />
  </React.StrictMode>
);
